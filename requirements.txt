qdrant-client>=1.9.0
scikit-learn>=1.4.0
numpy>=1.26.0
requests>=2.31.0
python-dotenv>=1.0.0
tqdm>=4.66.0
openai>=1.30.0
gradio>=4.0.0
redis>=5.0.0
# torch (optional) — enables GPU-accelerated BM25 hashing
# Python 3.14 + CUDA 12.6 nightly (works on CUDA 13.x drivers via backward compat):
#   pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cu126
